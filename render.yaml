services:
  # Monolithic Full-Stack Web Service
  - type: web
    name: mediconnect-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install --include=dev && cd ../frontend && npm install --include=dev && npm run build && cd ../backend && rm -rf ./dist && cp -r ../frontend/dist ./dist && npx prisma generate && npx prisma db push && npx prisma db seed
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mediconnect-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: NODE_ENV
        value: production

databases:
  - name: mediconnect-db
    plan: free
